<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.draughts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginScreen"
            android:label="@string/label_login"
            android:finishOnTaskLaunch="true">
        </activity>

        <activity android:name=".ManagementScreen"
            android:label="@string/menu_label">
        </activity>

        <activity android:name=".RecordTableScreen"
            android:label="@string/Table_records">
        </activity>

        <activity android:name=".ComputerLevelWin"
            android:label="@string/Table_records">
        </activity>

        <activity android:name=".GameWinAgainstBot"
            android:label="@string/Table_records">
        </activity>

        <activity android:name=".GameWinAgainstUser"
            android:label="@string/Table_records">
        </activity>
    </application>

</manifest>